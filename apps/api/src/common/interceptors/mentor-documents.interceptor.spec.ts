import { MentorDocumentsInterceptor } from './mentor-documents.interceptor';

describe('MentorDocumentsInterceptor', () => {
  it('should be defined', () => {
    expect(new MentorDocumentsInterceptor()).toBeDefined();
  });
});
