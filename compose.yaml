services:
  postgres:
    image: postgres:17
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: gurokonekt
      POSTGRES_PASSWORD: gurokonekt
      POSTGRES_DB: gurokonekt
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: